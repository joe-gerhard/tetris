export const COLORS = [
    'black',
    'red',
    'orange',
    'yellow',
    'green',
    'blue',
    'indigo',
    'violet',
];

export const BASE_SCORES = [0, 40, 100, 300, 1200];

export const ROWS = 20;
export const COLUMNS = 10;
export const SQUARESIZE = (document.body.offsetHeight - 250) / ROWS;
export const WIDTH = COLUMNS * SQUARESIZE;
export const HEIGHT = ROWS * SQUARESIZE;
